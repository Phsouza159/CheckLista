
AtlasApp.core.importModules('form', function (self) {

    console.log('form');

    /*
    var nmForm = '', dataForm, name, key, keys;
 
    nmForm = dataName;
 
    if (!dataName.includes('#'))
        dataName = '#' + dataName;
 
    dataForm = this.form.data[nmForm];
    keys = Object.keys(dataForm);
 
    for (key of keys) {
        $(dataName + ' [name=' + key + ']').val(dataForm[key]);
    }
    */
});

